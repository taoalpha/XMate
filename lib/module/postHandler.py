# define the allIn for post
def allIn(request,res=None):
    res["status"] = "successful"
    return res

# define the profile update for post
def userProfile(request,res=None):
    res["status"] = "successful"
    return res
